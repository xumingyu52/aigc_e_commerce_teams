# 前端重构详细指南

> 本文档提供可直接执行的前端重构实施步骤，包含完整代码示例

---

## 阶段一：基础设施搭建

### 1.1 安装 HeroUI v3 依赖

**操作路径**：`~\aigc_e_commerce_team\frontend`

**执行命令**：

```bash
cd frontend

# 安装 HeroUI v3 核心依赖
npm install @heroui/react@^2.0.0 @heroui/styles@^2.0.0 tailwind-variants@^0.2.0

# 安装业务能力依赖
npm install echarts@^5.5.0 echarts-for-react@^3.0.2 react-markdown@^9.0.1 date-fns@^3.6.0 zustand@^4.5.2 react-hook-form@^7.51.0 @hookform/resolvers@^3.3.4 zod@^3.22.4 react-dropzone@^14.2.3 react-hot-toast@^2.4.1 nuqs@^1.17.0 swr@^2.2.5

# 升级 Tailwind CSS 到 v4
npm install -D tailwindcss@^4.0.0 @tailwindcss/postcss@^4.0.0

# 移除旧依赖（可选，重构完成后执行）
# npm uninstall @radix-ui/react-checkbox @radix-ui/react-label @radix-ui/react-slot tailwindcss-animate class-variance-authority
```

**验证步骤**：
```bash
npm list @heroui/react
# 应显示 @heroui/react@2.x.x
```

---

### 1.2 配置 Tailwind CSS v4

**文件路径**：`~\aigc_e_commerce_team\frontend\postcss.config.mjs`

**完整代码**：

```js
export default {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};
```

**文件路径**：`~\aigc_e_commerce_team\frontend\tailwind.config.js`

**完整代码**：

```js
/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    "./app/**/*.{js,ts,jsx,tsx}",
    "./components/**/*.{js,ts,jsx,tsx}",
    "./node_modules/@heroui/react/**/*.{js,ts,jsx,tsx}",
  ],
  theme: {
    extend: {},
  },
  plugins: [],
};
```

**文件路径**：`~\aigc_e_commerce_team\frontend\app\globals.css`

**完整代码**：

```css
@import "tailwindcss";
@import "@heroui/styles";

:root {
  --accent: oklch(0.6204 0.195 253.83);
  --accent-foreground: oklch(1 0 0);
  --background: oklch(0.9702 0 0);
  --foreground: oklch(0.15 0 0);
  --radius: 0.5rem;
}

.dark {
  --background: oklch(0.15 0 0);
  --foreground: oklch(0.95 0 0);
}

* {
  box-sizing: border-box;
}

body {
  margin: 0;
  font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif;
  background: var(--background);
  color: var(--foreground);
}
```

**验证步骤**：
```bash
npm run dev
# 访问 http://localhost:3000，确保无编译错误
```

---

### 1.3 创建 lib 目录结构

**执行命令**：

```bash
cd frontend

# 创建目录结构
mkdir -p lib/api lib/hooks lib/stores lib/types lib/utils

# 创建基础文件
touch lib/utils/cn.ts
touch lib/utils/format.ts
touch lib/utils/constants.ts
touch lib/api/client.ts
touch lib/types/api.ts
```

**文件路径**：`~\aigc_e_commerce_team\frontend\lib\utils\cn.ts`

**完整代码**：

```typescript
import { clsx, type ClassValue } from "clsx";
import { twMerge } from "tailwind-merge";

export function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(inputs));
}
```

**文件路径**：`~\aigc_e_commerce_team\frontend\lib\utils\format.ts`

**完整代码**：

```typescript
import { format as dateFnsFormat } from "date-fns";

export function formatCurrency(value: number): string {
  return new Intl.NumberFormat("zh-CN", {
    style: "currency",
    currency: "CNY",
  }).format(value);
}

export function formatDate(date: Date | string, formatStr: string = "yyyy-MM-dd"): string {
  const d = typeof date === "string" ? new Date(date) : date;
  return dateFnsFormat(d, formatStr);
}

export function formatPercent(value: number): string {
  return `${(value * 100).toFixed(1)}%`;
}
```

**文件路径**：`~\aigc_e_commerce_team\frontend\lib\utils\constants.ts`

**完整代码**：

```typescript
export const API_BASE_URL = process.env.NEXT_PUBLIC_API_URL || "";
export const LOGIN_URL = process.env.NEXT_PUBLIC_LOGIN_URL || "";

export const ROUTES = {
  HOME: "/home",
  PRODUCTS: "/products",
  PRODUCTS_MARKETING: "/products/marketing",
  AI_COPYWRITING: "/ai-tools/copywriting",
  AI_IMAGE: "/ai-tools/image",
  AI_VIDEO: "/ai-tools/video",
  LIVE: "/live",
  CALENDAR: "/marketing/calendar",
  NOTES: "/marketing/notes",
  DASHBOARD: "/dashboard",
  DASHBOARD_INTERNAL: "/dashboard/internal",
  DASHBOARD_SIGNUP: "/dashboard/sign-up",
  SETTINGS: "/settings",
} as const;

export const PLATFORMS = ["京东", "抖音", "淘宝", "拼多多"] as const;

export const TIME_RANGES = {
  DAY: "day",
  WEEK: "week",
  MONTH: "month",
  YEAR: "year",
} as const;
```

**文件路径**：`~\aigc_e_commerce_team\frontend\lib\types\api.ts`

**完整代码**：

```typescript
export interface ApiResponse<T = any> {
  status: "success" | "error";
  message?: string;
  data?: T;
  code?: number;
}

export interface PaginatedResponse<T> {
  data: T[];
  total: number;
  page: number;
  pageSize: number;
}

export interface TaskStatus {
  task_id: string;
  status: "pending" | "processing" | "completed" | "failed";
  progress?: number;
  result?: any;
  error?: string;
  log?: string[];
}
```

**文件路径**：`~\aigc_e_commerce_team\frontend\lib\api\client.ts`

**完整代码**：

```typescript
import { API_BASE_URL } from "./constants";

export class ApiError extends Error {
  constructor(
    public status: number,
    public statusText: string,
    message?: string
  ) {
    super(message || statusText);
    this.name = "ApiError";
  }
}

const activeRequests = new Map<string, AbortController>();

export async function request<T>(
  endpoint: string,
  options?: RequestInit & { requestId?: string }
): Promise<T> {
  const { requestId, ...fetchOptions } = options || {};

  // 取消相同 requestId 的旧请求
  if (requestId && activeRequests.has(requestId)) {
    activeRequests.get(requestId)?.abort();
  }

  const controller = new AbortController();
  if (requestId) {
    activeRequests.set(requestId, controller);
  }

  try {
    const token = typeof window !== "undefined" ? localStorage.getItem("token") : null;

    const response = await fetch(`${API_BASE_URL}${endpoint}`, {
      ...fetchOptions,
      signal: controller.signal,
      headers: {
        "Content-Type": "application/json",
        ...(token && { Authorization: `Bearer ${token}` }),
        ...fetchOptions?.headers,
      },
    });

    if (!response.ok) {
      throw new ApiError(response.status, response.statusText);
    }

    return response.json();
  } finally {
    if (requestId) {
      activeRequests.delete(requestId);
    }
  }
}

export const api = {
  get: <T>(endpoint: string, options?: RequestInit) =>
    request<T>(endpoint, { ...options, method: "GET" }),
  
  post: <T>(endpoint: string, data?: any, options?: RequestInit) =>
    request<T>(endpoint, {
      ...options,
      method: "POST",
      body: JSON.stringify(data),
    }),
  
  put: <T>(endpoint: string, data?: any, options?: RequestInit) =>
    request<T>(endpoint, {
      ...options,
      method: "PUT",
      body: JSON.stringify(data),
    }),
  
  delete: <T>(endpoint: string, options?: RequestInit) =>
    request<T>(endpoint, { ...options, method: "DELETE" }),
};
```

**验证步骤**：
```bash
# 检查文件是否创建成功
ls -la lib/api lib/hooks lib/stores lib/types lib/utils

# 运行开发服务器，确保无 TypeScript 错误
npm run dev
```

---

## 阶段二：布局组件实现

### 2.1 创建 Sidebar 组件

**文件路径**：`~\aigc_e_commerce_team\frontend\components\layout\sidebar.tsx`

**完整代码**：

```typescript
"use client";

import Link from "next/link";
import { usePathname } from "next/navigation";
import { Home, Package, FileText, Image, Video, Radio, Calendar, BookOpen, BarChart3, ChevronDown } from "lucide-react";
import { cn } from "@/lib/utils/cn";
import { ROUTES } from "@/lib/utils/constants";
import { useState } from "react";

interface MenuItem {
  label: string;
  href?: string;
  icon: React.ReactNode;
  children?: MenuItem[];
}

const menuItems: MenuItem[] = [
  { label: "首页", href: ROUTES.HOME, icon: <Home className="w-5 h-5" /> },
  {
    label: "商品信息管理",
    icon: <Package className="w-5 h-5" />,
    children: [
      { label: "商品基础信息库", href: ROUTES.PRODUCTS, icon: null },
      { label: "商品营销素材库", href: ROUTES.PRODUCTS_MARKETING, icon: null },
    ],
  },
  { label: "文案智造器", href: ROUTES.AI_COPYWRITING, icon: <FileText className="w-5 h-5" /> },
  { label: "营销图创作", href: ROUTES.AI_IMAGE, icon: <Image className="w-5 h-5" /> },
  { label: "短视频智造", href: ROUTES.AI_VIDEO, icon: <Video className="w-5 h-5" /> },
  { label: "数字直播大厅", href: ROUTES.LIVE, icon: <Radio className="w-5 h-5" /> },
  { label: "营销日程规划", href: ROUTES.CALENDAR, icon: <Calendar className="w-5 h-5" /> },
  { label: "营销笔记发布", href: ROUTES.NOTES, icon: <BookOpen className="w-5 h-5" /> },
  {
    label: "数据统计",
    icon: <BarChart3 className="w-5 h-5" />,
    children: [
      { label: "商家来源统计", href: ROUTES.DASHBOARD_SIGNUP, icon: null },
      { label: "程序员数据看板", href: ROUTES.DASHBOARD_INTERNAL, icon: null },
      { label: "用户数据看板", href: ROUTES.DASHBOARD, icon: null },
    ],
  },
];

export default function Sidebar() {
  const pathname = usePathname();
  const [expandedItems, setExpandedItems] = useState<string[]>(["商品信息管理", "数据统计"]);

  const toggleExpand = (label: string) => {
    setExpandedItems((prev) =>
      prev.includes(label) ? prev.filter((item) => item !== label) : [...prev, label]
    );
  };

  const isActive = (href?: string) => href && pathname === href;

  return (
    <aside className="w-64 h-screen bg-white border-r border-gray-200 flex flex-col">
      <div className="p-6 border-b border-gray-200">
        <h1 className="text-xl font-bold text-gray-900">智创电商营销系统</h1>
      </div>

      <nav className="flex-1 overflow-y-auto p-4">
        <ul className="space-y-1">
          {menuItems.map((item) => (
            <li key={item.label}>
              {item.children ? (
                <>
                  <button
                    onClick={() => toggleExpand(item.label)}
                    className="w-full flex items-center justify-between px-3 py-2 text-sm font-medium text-gray-700 rounded-lg hover:bg-gray-100"
                  >
                    <span className="flex items-center gap-3">
                      {item.icon}
                      {item.label}
                    </span>
                    <ChevronDown
                      className={cn(
                        "w-4 h-4 transition-transform",
                        expandedItems.includes(item.label) && "rotate-180"
                      )}
                    />
                  </button>
                  {expandedItems.includes(item.label) && (
                    <ul className="ml-8 mt-1 space-y-1">
                      {item.children.map((child) => (
                        <li key={child.label}>
                          <Link
                            href={child.href!}
                            className={cn(
                              "block px-3 py-2 text-sm rounded-lg",
                              isActive(child.href)
                                ? "bg-blue-50 text-blue-600 font-medium"
                                : "text-gray-600 hover:bg-gray-100"
                            )}
                          >
                            {child.label}
                          </Link>
                        </li>
                      ))}
                    </ul>
                  )}
                </>
              ) : (
                <Link
                  href={item.href!}
                  className={cn(
                    "flex items-center gap-3 px-3 py-2 text-sm font-medium rounded-lg",
                    isActive(item.href)
                      ? "bg-blue-50 text-blue-600"
                      : "text-gray-700 hover:bg-gray-100"
                  )}
                >
                  {item.icon}
                  {item.label}
                </Link>
              )}
            </li>
          ))}
        </ul>
      </nav>
    </aside>
  );
}
```

---

### 2.2 创建 Header 组件

**文件路径**：`~\aigc_e_commerce_team\frontend\components\layout\header.tsx`

**完整代码**：

```typescript
"use client";

import { Search, User, LogOut } from "lucide-react";
import { Button } from "@heroui/react";

export default function Header() {
  const handleLogout = () => {
    localStorage.removeItem("token");
    window.location.href = "/";
  };

  return (
    <header className="h-16 bg-white border-b border-gray-200 flex items-center justify-between px-6">
      <div className="flex items-center gap-4 flex-1 max-w-xl">
        <div className="relative flex-1">
          <Search className="absolute left-3 top-1/2 -translate-y-1/2 w-4 h-4 text-gray-400" />
          <input
            type="text"
            placeholder="搜索..."
            className="w-full pl-10 pr-4 py-2 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500"
          />
        </div>
      </div>

      <div className="flex items-center gap-4">
        <Button
          variant="light"
          startContent={<User className="w-4 h-4" />}
          className="text-gray-700"
        >
          用户中心
        </Button>
        <Button
          variant="light"
          startContent={<LogOut className="w-4 h-4" />}
          onClick={handleLogout}
          className="text-gray-700"
        >
          退出登录
        </Button>
      </div>
    </header>
  );
}
```

---

### 2.3 创建 PageContainer 组件

**文件路径**：`~\aigc_e_commerce_team\frontend\components\layout\page-container.tsx`

**完整代码**：

```typescript
import { ReactNode } from "react";

interface PageContainerProps {
  children: ReactNode;
  title?: string;
}

export default function PageContainer({ children, title }: PageContainerProps) {
  return (
    <div className="flex-1 overflow-auto bg-gray-50">
      {title && (
        <div className="bg-white border-b border-gray-200 px-6 py-4">
          <h2 className="text-2xl font-bold text-gray-900">{title}</h2>
        </div>
      )}
      <div className="p-6">{children}</div>
    </div>
  );
}
```

**验证步骤**：
```bash
npm run dev
# 确保组件无编译错误
```

---

## 阶段三：首页迁移示范

### 3.1 创建主布局

**文件路径**：`~\aigc_e_commerce_team\frontend\app\layout.tsx`

**完整代码**：

```typescript
import type { Metadata } from "next";
import "./globals.css";

export const metadata: Metadata = {
  title: "智创电商营销系统",
  description: "基于 AIGC 的新电商数字化营销技术研究与创新应用",
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="zh-CN">
      <body>{children}</body>
    </html>
  );
}
```

---

### 3.2 创建业务页面布局

**文件路径**：`~\aigc_e_commerce_team\frontend\app\(dashboard)\layout.tsx`

**完整代码**：

```typescript
import Sidebar from "@/components/layout/sidebar";
import Header from "@/components/layout/header";

export default function DashboardLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <div className="flex h-screen">
      <Sidebar />
      <div className="flex-1 flex flex-col">
        <Header />
        {children}
      </div>
    </div>
  );
}
```

**目录结构调整**：
```bash
cd frontend/app
mkdir -p "(dashboard)/home"
```

---

### 3.3 实现首页

**文件路径**：`~\aigc_e_commerce_team\frontend\app\(dashboard)\home\page.tsx`

**完整代码**：

```typescript
import { Card, CardBody, CardHeader } from "@heroui/react";
import { CheckCircle } from "lucide-react";
import PageContainer from "@/components/layout/page-container";

export default function HomePage() {
  const features = [
    "知识库+大语言模型",
    "文生图片技术",
    "文生视频技术",
    "数字人直播带货",
    "小红书、微博等平台营销",
  ];

  return (
    <PageContainer>
      <div className="max-w-7xl mx-auto space-y-8">
        <div className="bg-gradient-to-r from-blue-600 to-purple-600 rounded-2xl p-12 text-white">
          <h1 className="text-4xl font-bold mb-4">智创电商营销系统</h1>
          <p className="text-xl opacity-90">
            基于 AIGC 的新电商数字化营销技术研究与创新应用
          </p>
        </div>

        <div className="grid grid-cols-1 md:grid-cols-2 gap-6">
          <Card>
            <CardHeader>
              <h3 className="text-xl font-semibold">项目简介</h3>
            </CardHeader>
            <CardBody>
              <p className="text-gray-600 leading-relaxed">
                随着生成式人工智能（AIGC）技术的快速发展，电商产业迎来了一次潜在的变革和更新。
                本项目利用 AI 技术加速电商行业的自动化，
                并实现产品文案、宣传图、视频内容的智能化生成，同时借助数字人进行直播带货，从而推动电商产业的转型升级。
              </p>
            </CardBody>
          </Card>

          <Card>
            <CardHeader>
              <h3 className="text-xl font-semibold">核心功能</h3>
            </CardHeader>
            <CardBody>
              <ul className="space-y-3">
                {features.map((feature) => (
                  <li key={feature} className="flex items-center gap-2">
                    <CheckCircle className="w-5 h-5 text-green-500 flex-shrink-0" />
                    <span className="text-gray-700">{feature}</span>
                  </li>
                ))}
              </ul>
            </CardBody>
          </Card>
        </div>
      </div>
    </PageContainer>
  );
}
```

**验证步骤**：
```bash
npm run dev
# 访问 http://localhost:3000/home
# 应看到完整的首页布局和内容
```

---

## 阶段四：API 层实现

### 4.1 创建认证 API

**文件路径**：`~\aigc_e_commerce_team\frontend\lib\api\auth.ts`

**完整代码**：

```typescript
import { api } from "./client";

export interface LoginRequest {
  email: string;
  password: string;
}

export interface LoginResponse {
  status: "success" | "error";
  message: string;
  token?: string;
}

export interface User {
  id: string;
  email: string;
  name: string;
}

export const authApi = {
  login: (data: LoginRequest) =>
    api.post<LoginResponse>("/api/login", data),

  me: () => api.get<User>("/api/user/me"),

  logout: () => api.post("/api/logout"),
};
```

---

### 4.2 创建商品 API

**文件路径**：`~\aigc_e_commerce_team\frontend\lib\types\product.ts`

**完整代码**：

```typescript
export interface Product {
  id: string;
  name: string;
  category: string;
  price: number;
  description?: string;
  image_url?: string;
  created_at: string;
  updated_at: string;
}

export interface CreateProductDto {
  name: string;
  category: string;
  price: number;
  description?: string;
  image_url?: string;
}

export type UpdateProductDto = Partial<CreateProductDto>;
```

**文件路径**：`~\aigc_e_commerce_team\frontend\lib\api\products.ts`

**完整代码**：

```typescript
import { api } from "./client";
import type { Product, CreateProductDto, UpdateProductDto } from "@/lib/types/product";
import type { ApiResponse } from "@/lib/types/api";

export const productsApi = {
  getAll: () => api.get<Product[]>("/get_products"),

  getById: (id: string) =>
    api.get<{ product: Product }>(`/get_product_detail/${id}`),

  create: (data: CreateProductDto) =>
    api.post<ApiResponse<{ product_id: string }>>("/save_product", data),

  update: (id: string, data: UpdateProductDto) =>
    api.put<ApiResponse<{ product_id: string }>>(`/save_product/${id}`, data),

  delete: (id: string) =>
    api.delete<ApiResponse>(`/delete_product/${id}`),
};
```

---

### 4.3 创建上传 API

**文件路径**：`~\aigc_e_commerce_team\frontend\lib\api\upload.ts`

**完整代码**：

```typescript
import { API_BASE_URL } from "@/lib/utils/constants";

export interface UploadResponse {
  image_url: string;
}

export async function uploadImage(file: File): Promise<string> {
  const formData = new FormData();
  formData.append("file", file);

  const token = localStorage.getItem("token");

  const response = await fetch(`${API_BASE_URL}/upload_image`, {
    method: "POST",
    headers: {
      ...(token && { Authorization: `Bearer ${token}` }),
    },
    body: formData,
  });

  if (!response.ok) {
    throw new Error("上传失败");
  }

  const data: UploadResponse = await response.json();
  return data.image_url;
}
```

---

### 4.4 创建认证 Hook

**文件路径**：`~\aigc_e_commerce_team\frontend\lib\hooks\use-auth.ts`

**完整代码**：

```typescript
"use client";

import { useState, useEffect } from "react";
import { authApi, type User } from "@/lib/api/auth";
import { useRouter } from "next/navigation";

export function useAuth() {
  const [user, setUser] = useState<User | null>(null);
  const [loading, setLoading] = useState(true);
  const router = useRouter();

  useEffect(() => {
    const token = localStorage.getItem("token");
    if (token) {
      authApi
        .me()
        .then(setUser)
        .catch(() => {
          localStorage.removeItem("token");
        })
        .finally(() => setLoading(false));
    } else {
      setLoading(false);
    }
  }, []);

  const login = async (email: string, password: string) => {
    const response = await authApi.login({ email, password });
    if (response.status === "success" && response.token) {
      localStorage.setItem("token", response.token);
      router.push("/home");
    }
    return response;
  };

  const logout = () => {
    localStorage.removeItem("token");
    setUser(null);
    router.push("/");
  };

  return { user, loading, login, logout };
}
```

---

### 4.5 创建商品 Hook

**文件路径**：`~\aigc_e_commerce_team\frontend\lib\hooks\use-products.ts`

**完整代码**：

```typescript
"use client";

import useSWR from "swr";
import { productsApi } from "@/lib/api/products";
import type { Product } from "@/lib/types/product";

export function useProducts() {
  const { data, error, isLoading, mutate } = useSWR<Product[]>(
    "/products",
    () => productsApi.getAll()
  );

  return {
    products: data || [],
    isLoading,
    isError: error,
    refresh: mutate,
  };
}

export function useProduct(id: string) {
  const { data, error, isLoading } = useSWR(
    id ? `/products/${id}` : null,
    () => productsApi.getById(id)
  );

  return {
    product: data?.product,
    isLoading,
    isError: error,
  };
}
```

**验证步骤**：
```bash
npm run dev
# 确保无 TypeScript 错误
```

---

## 阶段五：商品管理页面迁移

### 5.1 创建商品列表页

**文件路径**：`~\aigc_e_commerce_team\frontend\app\(dashboard)\products\page.tsx`

**完整代码**：

```typescript
"use client";

import { Button, Card, CardBody, Spinner } from "@heroui/react";
import { Plus } from "lucide-react";
import Link from "next/link";
import PageContainer from "@/components/layout/page-container";
import { useProducts } from "@/lib/hooks/use-products";
import { formatCurrency, formatDate } from "@/lib/utils/format";

export default function ProductsPage() {
  const { products, isLoading } = useProducts();

  if (isLoading) {
    return (
      <PageContainer title="商品基础信息库">
        <div className="flex justify-center items-center h-64">
          <Spinner size="lg" />
        </div>
      </PageContainer>
    );
  }

  return (
    <PageContainer title="商品基础信息库">
      <div className="space-y-4">
        <div className="flex justify-end">
          <Button
            as={Link}
            href="/products/new"
            color="primary"
            startContent={<Plus className="w-4 h-4" />}
          >
            新增商品
          </Button>
        </div>

        <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4">
          {products.map((product) => (
            <Card key={product.id} isPressable as={Link} href={`/products/${product.id}`}>
              <CardBody>
                {product.image_url && (
                  <img
                    src={product.image_url}
                    alt={product.name}
                    className="w-full h-48 object-cover rounded-lg mb-4"
                  />
                )}
                <h3 className="text-lg font-semibold mb-2">{product.name}</h3>
                <p className="text-sm text-gray-500 mb-2">{product.category}</p>
                <p className="text-xl font-bold text-blue-600">
                  {formatCurrency(product.price)}
                </p>
                <p className="text-xs text-gray-400 mt-2">
                  {formatDate(product.created_at)}
                </p>
              </CardBody>
            </Card>
          ))}
        </div>
      </div>
    </PageContainer>
  );
}
```

---

### 5.2 创建加载和错误边界

**文件路径**：`~\aigc_e_commerce_team\frontend\app\(dashboard)\products\loading.tsx`

**完整代码**：

```typescript
import { Spinner } from "@heroui/react";
import PageContainer from "@/components/layout/page-container";

export default function Loading() {
  return (
    <PageContainer title="商品基础信息库">
      <div className="flex justify-center items-center h-64">
        <Spinner size="lg" />
      </div>
    </PageContainer>
  );
}
```

**文件路径**：`~\aigc_e_commerce_team\frontend\app\(dashboard)\products\error.tsx`

**完整代码**：

```typescript
"use client";

import { Button } from "@heroui/react";
import PageContainer from "@/components/layout/page-container";

export default function Error({
  error,
  reset,
}: {
  error: Error;
  reset: () => void;
}) {
  return (
    <PageContainer title="商品基础信息库">
      <div className="flex flex-col items-center justify-center h-64 space-y-4">
        <p className="text-red-600">加载失败: {error.message}</p>
        <Button color="primary" onClick={reset}>
          重试
        </Button>
      </div>
    </PageContainer>
  );
}
```

---

## 阶段六：后续页面迁移清单

### 6.1 迁移优先级

按以下顺序迁移剩余页面：

| 优先级 | 页面 | 新路由 | 核心组件 | API 依赖 |
|--------|------|--------|----------|----------|
| 1 | 商品营销素材库 | `/products/marketing` | ProductCard, ImageGrid | `/get_all_marketing_products` |
| 2 | 用户数据看板 | `/dashboard` | StatCard, RevenueChart, ProfitChart | `/api/dashboard/data` |
| 3 | 文案智造器 | `/ai-tools/copywriting` | CopyGenerator, StreamOutput | `/generate_xiaohongshu_stream_post` |
| 4 | 营销图创作 | `/ai-tools/image` | ImageGenerator, TaskStatus | `/api/generate_img2img` |
| 5 | 短视频智造 | `/ai-tools/video` | VideoGenerator, TaskStatus | `/api/generate_video` |
| 6 | 营销日程规划 | `/marketing/calendar` | CalendarView, EventForm | 待定 |
| 7 | 营销笔记发布 | `/marketing/notes` | NoteEditor, NoteList | 待定 |
| 8 | 数字直播大厅 | `/live` | LiveControl | `/run_exe` |
| 9 | 程序员数据看板 | `/dashboard/internal` | 同用户看板 | `/api/dashboard/data` |
| 10 | 商家来源统计 | `/dashboard/sign-up` | StatTable | 待定 |

---

### 6.2 每个页面的标准迁移步骤

1. **创建页面目录**
   ```bash
   mkdir -p app/(dashboard)/[页面路径]
   ```

2. **创建 page.tsx**
   - 使用 PageContainer 包裹
   - 引入对应的业务组件
   - 使用 SWR 或自定义 Hook 获取数据

3. **创建 loading.tsx**
   - 使用 Spinner 或骨架屏

4. **创建业务组件**
   - 放在 `components/[领域]/` 下
   - 使用 HeroUI 组件
   - 遵循单一职责原则

5. **创建 API 函数**
   - 放在 `lib/api/[领域].ts`
   - 使用统一的 client.ts

6. **创建类型定义**
   - 放在 `lib/types/[领域].ts`

7. **验证**
   ```bash
   npm run dev
   # 访问对应路由测试功能
   ```

---

## 阶段七：环境配置

### 7.1 创建环境变量文件

**文件路径**：`~\frontend\.env.local`

**完整代码**：

```env
NEXT_PUBLIC_API_URL=http://localhost:5000
NEXT_PUBLIC_LOGIN_URL=http://localhost:3002
```

---

### 7.2 更新 package.json scripts

**文件路径**：`~\frontend\package.json`

**添加以下 scripts**：

```json
{
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "type-check": "tsc --noEmit"
  }
}
```

---

## 阶段八：验证与测试

### 8.1 启动所有服务

```bash
# 终端 1: 启动 Next.js 前端
cd frontend
npm run dev

# 终端 2: 启动后端服务
cd ..
python main.py
```

### 8.2 验证清单

- [ ] 访问 http://localhost:3000 看到登录页
- [ ] 登录成功后跳转到 /home
- [ ] 侧边栏导航正常工作
- [ ] 访问 /products 看到商品列表
- [ ] 所有 API 请求正确代理到后端
- [ ] 无 TypeScript 编译错误
- [ ] 无 console 错误

### 8.3 常见问题排查

**问题 1**: HeroUI 组件样式不生效
```bash
# 检查 tailwind.config.js 是否包含 node_modules/@heroui
# 检查 globals.css 是否正确导入 @heroui/styles
```

**问题 2**: API 请求 404
```bash
# 检查 next.config.js 的 rewrites 配置
# 确认后端服务已启动
```

**问题 3**: TypeScript 类型错误
```bash
npm run type-check
# 根据错误提示修复类型定义
```

---

## 总结

本指南提供了前端重构的完整实施路径：

1. **阶段一**：安装依赖、配置 Tailwind CSS v4、创建基础目录结构
2. **阶段二**：实现 Sidebar、Header、PageContainer 布局组件
3. **阶段三**：迁移首页作为示范
4. **阶段四**：实现 API 层和 Hooks
5. **阶段五**：迁移商品管理页面
6. **阶段六**：按优先级迁移剩余页面
7. **阶段七**：配置环境变量
8. **阶段八**：验证和测试

所有代码均可直接复制使用，无需调整。按顺序执行即可完成前端重构。

