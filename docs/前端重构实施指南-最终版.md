# 前端重构实施指南（最终版）

> 基于 HeroUI v3 + Tailwind CSS v4 + Next.js 14 App Router 的完整重构方案
> 
> 本指南旨在引导 AI 进行系统化重构，包含完整阶段划分、规范约束、最佳实践和验证步骤

---

## 📋 目录

- [阶段 0：诊断与准备](#阶段-0诊断与准备)
- [阶段 1：依赖安装与配置](#阶段-1依赖安装与配置)
- [阶段 2：基础设施层](#阶段-2基础设施层)
- [阶段 3：布局组件层](#阶段-3布局组件层)
- [阶段 4：路由架构](#阶段-4路由架构)
- [阶段 5：示范页面](#阶段-5示范页面)
- [阶段 6：API 与数据层](#阶段-6api-与数据层)
- [阶段 7：业务组件层](#阶段-7业务组件层)
- [阶段 8：页面迁移](#阶段-8页面迁移)
- [阶段 9：性能优化](#阶段-9性能优化)
- [阶段 10：验证与部署](#阶段-10验证与部署)

---

## 🎯 重构目标

### 技术栈迁移

| 维度 | 当前 | 目标 |
|------|------|------|
| 页面承载 | Flask 模板 + Next.js 登录页 | Next.js App Router 统一 |
| UI 组件库 | shadcn/ui + Radix UI + Bootstrap | **HeroUI v3** |
| CSS 框架 | Tailwind CSS v3.4.17 | **Tailwind CSS v4** |
| 状态管理 | jQuery 局部状态 | React Hooks + SWR + Zustand |
| 数据获取 | 模板内联脚本 | Server Components + SWR |

### 核心约束

1. ✅ **保留现有业务逻辑和 API 接口**
2. ✅ **保持后端服务不变**（3002/5000/5001）
3. ✅ **遵循 Next.js 14 官方最佳实践**
4. ✅ **遵循 Vercel React 性能优化指南**
5. ✅ **使用 HeroUI v3 复合组件模式**

---

## 阶段 0：诊断与准备

### 0.1 当前配置诊断

**执行诊断脚本**：

```bash
cd frontend

# 检查当前依赖版本
npm list tailwindcss @heroui/react next react

# 检查配置文件
ls -la | grep -E "(tailwind|postcss|next.config)"
```

**预期问题清单**：

| 问题 | 当前状态 | 目标状态 |
|------|----------|----------|
| Tailwind CSS 版本 | v3.4.17 | v4.0.0+ |
| PostCSS 配置 | `tailwindcss: {}` | `@tailwindcss/postcss` |
| tailwind.config.js | 存在（v3 格式） | **删除**（v4 不需要） |
| globals.css | `@tailwind base/components/utilities` | `@import "tailwindcss"` |
| HeroUI | 未安装 | `@heroui/react` + `@heroui/styles` |

### 0.2 备份现有代码

```bash
# 创建备份分支
git checkout -b backup-before-refactor
git add .
git commit -m "备份：重构前的代码状态"

# 创建重构分支
git checkout -b refactor-heroui-v3
```

### 0.3 阅读现有代码模式

**关键发现**（基于代码分析）：

1. **组件模式**：使用 `forwardRef` + `cva` variants
2. **API 调用**：直接 `fetch()`，无统一封装
3. **错误处理**：`try-catch` + `alert()`
4. **加载状态**：`useState(isLoading)` + 按钮 `disabled`

**迁移策略**：
- 保留 `forwardRef` 模式（与 HeroUI 兼容）
- 统一 API 调用到 `lib/api/`
- 替换 `alert()` 为 `react-hot-toast`
- 保持加载状态模式

### ✅ 验证步骤

```bash
# 确认备份完成
git log --oneline -5

# 确认在重构分支
git branch --show-current
# 应输出: refactor-heroui-v3
```

---

## 阶段 1：依赖安装与配置

### 1.1 清理冲突文件

```bash
cd frontend

# 删除 Tailwind v3 配置（v4 不需要）
rm tailwind.config.js

# 删除旧 PostCSS 配置
rm postcss.config.js
```

### 1.2 卸载旧依赖

```bash
# 卸载 Tailwind v3
npm uninstall tailwindcss

# 可选：卸载 shadcn/ui 相关依赖
npm uninstall @radix-ui/react-checkbox @radix-ui/react-label @radix-ui/react-slot tailwindcss-animate class-variance-authority
```

### 1.3 安装核心依赖

```bash
# Tailwind CSS v4
npm install -D tailwindcss@^4.0.0 @tailwindcss/postcss@^4.0.0

# HeroUI v3
npm install @heroui/react@^2.0.0 @heroui/styles@^2.0.0 tailwind-variants@^0.2.0

# 确保已安装工具库
npm list clsx tailwind-merge
# 如未安装：
# npm install clsx tailwind-merge
```

### 1.4 安装业务依赖

```bash
# 数据获取与状态管理
npm install swr@^2.2.5 zustand@^4.5.2

# 表单处理
npm install react-hook-form@^7.51.0 @hookform/resolvers@^3.3.4 zod@^3.22.4

# 图表与可视化
npm install echarts@^5.5.0 echarts-for-react@^3.0.2

# 其他工具
npm install react-markdown@^9.0.1 date-fns@^3.6.0 react-dropzone@^14.2.3 react-hot-toast@^2.4.1 nuqs@^1.17.0
```

### 1.5 配置 PostCSS（v4 格式）

**文件**：`frontend/postcss.config.mjs`

```js
export default {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};
```

### 1.6 配置 globals.css（v4 格式）

**文件**：`frontend/app/globals.css`

```css
/* 
 * Tailwind CSS v4 导入方式
 * 必须先导入 Tailwind，再导入 HeroUI
 */
@import "tailwindcss";

/* HeroUI v3 样式 */
@import "@heroui/styles";

/* 自定义主题变量（使用 oklch 色彩空间） */
:root {
  --primary: oklch(0.6204 0.195 253.83);
  --background: oklch(0.9702 0 0);
  --foreground: oklch(0.15 0 0);
  --radius: 0.5rem;
}

.dark {
  --background: oklch(0.15 0 0);
  --foreground: oklch(0.95 0 0);
}

/* 基础样式 */
* {
  box-sizing: border-box;
}

body {
  margin: 0;
  font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif;
  background: var(--background);
  color: var(--foreground);
}
```

### ✅ 验证步骤

```bash
# 验证依赖安装
npm list tailwindcss @heroui/react swr
# 应显示：
# tailwindcss@4.x.x
# @heroui/react@2.x.x
# swr@2.x.x

# 验证配置文件
cat postcss.config.mjs
cat app/globals.css

# 启动开发服务器
npm run dev
# 访问 http://localhost:3000
# 应无编译错误
```

---

## 阶段 2：基础设施层

### 2.1 创建目录结构

```bash
cd frontend

# 创建 lib 子目录
mkdir -p lib/api lib/hooks lib/stores lib/types lib/utils

# 创建组件目录
mkdir -p components/layout components/charts components/products components/ai-tools
```

### 2.2 工具函数层

#### cn 工具（className 合并）

**文件**：`frontend/lib/utils/cn.ts`

```typescript
import { clsx, type ClassValue } from "clsx";
import { twMerge } from "tailwind-merge";

/**
 * 合并 Tailwind CSS 类名，避免冲突
 * @example cn("px-2 py-1", "px-4") // => "py-1 px-4"
 */
export function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(inputs));
}
```

#### 格式化工具

**文件**：`frontend/lib/utils/format.ts`

```typescript
import { format as dateFnsFormat } from "date-fns";

/**
 * 格式化货币
 * @example formatCurrency(12345.67) // => "¥12,345.67"
 */
export function formatCurrency(value: number): string {
  return new Intl.NumberFormat("zh-CN", {
    style: "currency",
    currency: "CNY",
  }).format(value);
}

/**
 * 格式化日期
 * @example formatDate(new Date(), "yyyy-MM-dd HH:mm") // => "2026-03-23 16:30"
 */
export function formatDate(
  date: Date | string,
  formatStr: string = "yyyy-MM-dd"
): string {
  const d = typeof date === "string" ? new Date(date) : date;
  return dateFnsFormat(d, formatStr);
}

/**
 * 格式化百分比
 * @example formatPercent(0.1234) // => "12.3%"
 */
export function formatPercent(value: number): string {
  return `${(value * 100).toFixed(1)}%`;
}
```

#### 常量定义

**文件**：`frontend/lib/utils/constants.ts`

```typescript
export const API_BASE_URL = process.env.NEXT_PUBLIC_API_URL || "";

/**
 * 路由常量（使用 as const 确保类型安全）
 */
export const ROUTES = {
  HOME: "/home",
  PRODUCTS: "/products",
  PRODUCTS_MARKETING: "/products/marketing",
  AI_COPYWRITING: "/ai-tools/copywriting",
  AI_IMAGE: "/ai-tools/image",
  AI_VIDEO: "/ai-tools/video",
  LIVE: "/live",
  CALENDAR: "/marketing/calendar",
  NOTES: "/marketing/notes",
  DASHBOARD: "/dashboard",
  DASHBOARD_INTERNAL: "/dashboard/internal",
  DASHBOARD_SIGNUP: "/dashboard/sign-up",
  SETTINGS: "/settings",
} as const;

export const PLATFORMS = ["京东", "抖音", "淘宝", "拼多多"] as const;

export const TIME_RANGES = {
  DAY: "day",
  WEEK: "week",
  MONTH: "month",
  YEAR: "year",
} as const;
```

### 2.3 类型定义层

#### API 响应类型

**文件**：`frontend/lib/types/api.ts`

```typescript
/**
 * 通用 API 响应格式
 */
export interface ApiResponse<T = any> {
  status: "success" | "error";
  message?: string;
  data?: T;
  code?: number;
}

/**
 * 分页响应格式
 */
export interface PaginatedResponse<T> {
  data: T[];
  total: number;
  page: number;
  pageSize: number;
}

/**
 * 任务状态（用于 AI 生成任务）
 */
export interface TaskStatus {
  task_id: string;
  status: "pending" | "processing" | "completed" | "failed";
  progress?: number;
  result?: any;
  error?: string;
  log?: string[];
}
```

#### 商品类型

**文件**：`frontend/lib/types/product.ts`

```typescript
export interface Product {
  id: string;
  name: string;
  category: string;
  price: number;
  description?: string;
  image_url?: string;
  created_at: string;
  updated_at: string;
}

export interface CreateProductDto {
  name: string;
  category: string;
  price: number;
  description?: string;
  image_url?: string;
}

export type UpdateProductDto = Partial<CreateProductDto>;
```

### ✅ 验证步骤

```bash
# 检查目录结构
tree lib -L 2
# 应显示完整的 lib 目录结构

# 类型检查
npm run type-check
# 或
npx tsc --noEmit

# 应无类型错误
```

---

## 阶段 3：HTTP 客户端与 API 层

### 3.1 HTTP 客户端（遵循 Vercel 最佳实践）

**文件**：`frontend/lib/api/client.ts`

```typescript
import { API_BASE_URL } from "@/lib/utils/constants";

export class ApiError extends Error {
  constructor(
    public status: number,
    public statusText: string,
    message?: string
  ) {
    super(message || statusText);
    this.name = "ApiError";
  }
}

/**
 * 活动请求管理（防止重复请求）
 * 遵循 Vercel 最佳实践：client-event-listeners
 */
const activeRequests = new Map<string, AbortController>();

/**
 * 统一 HTTP 请求客户端
 * 
 * 特性：
 * - 自动注入 Token
 * - 请求去重（相同 requestId 会取消旧请求）
 * - 统一错误处理
 * - 支持请求取消
 * 
 * @example
 * const data = await request<User>("/api/user/me", { requestId: "get-user" })
 */
export async function request<T>(
  endpoint: string,
  options?: RequestInit & { requestId?: string }
): Promise<T> {
  const { requestId, ...fetchOptions } = options || {};

  // 取消相同 requestId 的旧请求（防止竞态条件）
  if (requestId && activeRequests.has(requestId)) {
    activeRequests.get(requestId)?.abort();
  }

  const controller = new AbortController();
  if (requestId) {
    activeRequests.set(requestId, controller);
  }

  try {
    const token = typeof window !== "undefined" ? localStorage.getItem("token") : null;

    const response = await fetch(`${API_BASE_URL}${endpoint}`, {
      ...fetchOptions,
      signal: controller.signal,
      headers: {
        "Content-Type": "application/json",
        ...(token && { Authorization: `Bearer ${token}` }),
        ...fetchOptions?.headers,
      },
    });

    if (!response.ok) {
      throw new ApiError(response.status, response.statusText);
    }

    return response.json();
  } finally {
    if (requestId) {
      activeRequests.delete(requestId);
    }
  }
}

/**
 * RESTful API 快捷方法
 */
export const api = {
  get: <T>(endpoint: string, options?: RequestInit) =>
    request<T>(endpoint, { ...options, method: "GET" }),
  
  post: <T>(endpoint: string, data?: any, options?: RequestInit) =>
    request<T>(endpoint, {
      ...options,
      method: "POST",
      body: JSON.stringify(data),
    }),
  
  put: <T>(endpoint: string, data?: any, options?: RequestInit) =>
    request<T>(endpoint, {
      ...options,
      method: "PUT",
      body: JSON.stringify(data),
    }),
  
  delete: <T>(endpoint: string, options?: RequestInit) =>
    request<T>(endpoint, { ...options, method: "DELETE" }),
};
```

### 3.2 认证 API

**文件**：`frontend/lib/api/auth.ts`

```typescript
import { api } from "./client";

export interface LoginRequest {
  email: string;
  password: string;
}

export interface LoginResponse {
  status: "success" | "error";
  message: string;
  token?: string;
}

export interface User {
  id: string;
  email: string;
  name: string;
}

/**
 * 认证相关 API
 * 注意：所有路径使用 /api/ 前缀（next.config.js 会代理到后端）
 */
export const authApi = {
  login: (data: LoginRequest) =>
    api.post<LoginResponse>("/api/login", data),

  me: () => api.get<User>("/api/user/me"),

  logout: () => api.post("/api/logout"),
};
```

### 3.3 商品 API

**文件**：`frontend/lib/api/products.ts`

```typescript
import { api } from "./client";
import type { Product, CreateProductDto, UpdateProductDto } from "@/lib/types/product";
import type { ApiResponse } from "@/lib/types/api";

/**
 * 商品管理 API
 * 遵循 RESTful 规范
 */
export const productsApi = {
  /**
   * 获取所有商品
   * 对应后端：GET /get_products
   */
  getAll: () => api.get<Product[]>("/api/get_products"),

  /**
   * 获取单个商品详情
   * 对应后端：GET /get_product_detail/:id
   */
  getById: (id: string) =>
    api.get<{ product: Product }>(`/api/get_product_detail/${id}`),

  /**
   * 创建商品
   * 对应后端：POST /save_product
   */
  create: (data: CreateProductDto) =>
    api.post<ApiResponse<{ product_id: string }>>("/api/save_product", data),

  /**
   * 更新商品
   * 对应后端：PUT /save_product/:id
   */
  update: (id: string, data: UpdateProductDto) =>
    api.put<ApiResponse<{ product_id: string }>>(`/api/save_product/${id}`, data),

  /**
   * 删除商品
   * 对应后端：DELETE /delete_product/:id
   */
  delete: (id: string) =>
    api.delete<ApiResponse>(`/api/delete_product/${id}`),
};
```

### 3.4 上传 API

**文件**：`frontend/lib/api/upload.ts`

```typescript
import { API_BASE_URL } from "@/lib/utils/constants";

export interface UploadResponse {
  image_url: string;
}

/**
 * 上传图片
 * 注意：使用 FormData，不设置 Content-Type（浏览器自动设置）
 * 
 * @example
 * const imageUrl = await uploadImage(file)
 */
export async function uploadImage(file: File): Promise<string> {
  const formData = new FormData();
  formData.append("file", file);

  const token = localStorage.getItem("token");

  const response = await fetch(`${API_BASE_URL}/api/upload_image`, {
    method: "POST",
    headers: {
      ...(token && { Authorization: `Bearer ${token}` }),
      // 不设置 Content-Type，让浏览器自动设置 multipart/form-data
    },
    body: formData,
  });

  if (!response.ok) {
    throw new Error("上传失败");
  }

  const data: UploadResponse = await response.json();
  return data.image_url;
}
```

### ✅ 验证步骤

```bash
# 类型检查
npm run type-check

# 测试 API 客户端（可选）
# 创建测试文件 lib/api/__tests__/client.test.ts
```

---

## 阶段 4：Hooks 层（遵循 React 最佳实践）

### 4.1 认证 Hook

**文件**：`frontend/lib/hooks/use-auth.ts`

```typescript
"use client";

import { useState, useEffect } from "react";
import { authApi, type User } from "@/lib/api/auth";
import { useRouter } from "next/navigation";

/**
 * 认证状态管理 Hook
 * 
 * 特性：
 * - 自动从 localStorage 恢复登录状态
 * - 提供 login/logout 方法
 * - 自动跳转
 * 
 * @example
 * const { user, loading, login, logout } = useAuth()
 */
export function useAuth() {
  const [user, setUser] = useState<User | null>(null);
  const [loading, setLoading] = useState(true);
  const router = useRouter();

  useEffect(() => {
    const token = localStorage.getItem("token");
    if (token) {
      authApi
        .me()
        .then(setUser)
        .catch(() => {
          localStorage.removeItem("token");
        })
        .finally(() => setLoading(false));
    } else {
      setLoading(false);
    }
  }, []);

  const login = async (email: string, password: string) => {
    const response = await authApi.login({ email, password });
    if (response.status === "success" && response.token) {
      localStorage.setItem("token", response.token);
      router.push("/home");
    }
    return response;
  };

  const logout = () => {
    localStorage.removeItem("token");
    setUser(null);
    router.push("/");
  };

  return { user, loading, login, logout };
}
```

### 4.2 商品 Hook（使用 SWR）

**文件**：`frontend/lib/hooks/use-products.ts`

```typescript
"use client";

import useSWR from "swr";
import { productsApi } from "@/lib/api/products";
import type { Product } from "@/lib/types/product";

/**
 * 商品列表 Hook
 * 使用 SWR 自动缓存和重新验证
 * 遵循 Vercel 最佳实践：client-swr-dedup
 * 
 * @example
 * const { products, isLoading, refresh } = useProducts()
 */
export function useProducts() {
  const { data, error, isLoading, mutate } = useSWR<Product[]>(
    "/products",
    () => productsApi.getAll(),
    {
      revalidateOnFocus: false, // 避免频繁重新验证
      dedupingInterval: 5000,   // 5秒内相同请求去重
    }
  );

  return {
    products: data || [],
    isLoading,
    isError: error,
    refresh: mutate,
  };
}

/**
 * 单个商品 Hook
 * 
 * @example
 * const { product, isLoading } = useProduct(id)
 */
export function useProduct(id: string) {
  const { data, error, isLoading } = useSWR(
    id ? `/products/${id}` : null,
    () => productsApi.getById(id)
  );

  return {
    product: data?.product,
    isLoading,
    isError: error,
  };
}
```

### ✅ 验证步骤

```bash
npm run type-check
# 应无类型错误
```

---

## 阶段 5：布局组件层（HeroUI v3 复合组件）

### 5.1 创建布局目录

```bash
mkdir -p frontend/components/layout
```

### 5.2 Sidebar 组件

**文件**：`frontend/components/layout/sidebar.tsx`

```typescript
"use client";

import Link from "next/link";
import { usePathname } from "next/navigation";
import { Home, Package, FileText, Image, Video, Radio, Calendar, BookOpen, BarChart3, ChevronDown } from "lucide-react";
import { cn } from "@/lib/utils/cn";
import { ROUTES } from "@/lib/utils/constants";
import { useState } from "react";

interface MenuItem {
  label: string;
  href?: string;
  icon: React.ReactNode;
  children?: MenuItem[];
}

const menuItems: MenuItem[] = [
  { label: "首页", href: ROUTES.HOME, icon: <Home className="w-5 h-5" /> },
  {
    label: "商品信息管理",
    icon: <Package className="w-5 h-5" />,
    children: [
      { label: "商品基础信息库", href: ROUTES.PRODUCTS, icon: null },
      { label: "商品营销素材库", href: ROUTES.PRODUCTS_MARKETING, icon: null },
    ],
  },
  { label: "文案智造器", href: ROUTES.AI_COPYWRITING, icon: <FileText className="w-5 h-5" /> },
  { label: "营销图创作", href: ROUTES.AI_IMAGE, icon: <Image className="w-5 h-5" /> },
  { label: "短视频智造", href: ROUTES.AI_VIDEO, icon: <Video className="w-5 h-5" /> },
  { label: "数字直播大厅", href: ROUTES.LIVE, icon: <Radio className="w-5 h-5" /> },
  { label: "营销日程规划", href: ROUTES.CALENDAR, icon: <Calendar className="w-5 h-5" /> },
  { label: "营销笔记发布", href: ROUTES.NOTES, icon: <BookOpen className="w-5 h-5" /> },
  {
    label: "数据统计",
    icon: <BarChart3 className="w-5 h-5" />,
    children: [
      { label: "商家来源统计", href: ROUTES.DASHBOARD_SIGNUP, icon: null },
      { label: "程序员数据看板", href: ROUTES.DASHBOARD_INTERNAL, icon: null },
      { label: "用户数据看板", href: ROUTES.DASHBOARD, icon: null },
    ],
  },
];

export default function Sidebar() {
  const pathname = usePathname();
  const [expandedItems, setExpandedItems] = useState<string[]>(["商品信息管理", "数据统计"]);

  const toggleExpand = (label: string) => {
    setExpandedItems((prev) =>
      prev.includes(label) ? prev.filter((item) => item !== label) : [...prev, label]
    );
  };

  const isActive = (href?: string) => href && pathname === href;

  return (
    <aside className="w-64 h-screen bg-white border-r border-gray-200 flex flex-col">
      <div className="p-6 border-b border-gray-200">
        <h1 className="text-xl font-bold text-gray-900">智创电商营销系统</h1>
      </div>

      <nav className="flex-1 overflow-y-auto p-4">
        <ul className="space-y-1">
          {menuItems.map((item) => (
            <li key={item.label}>
              {item.children ? (
                <>
                  <button
                    onClick={() => toggleExpand(item.label)}
                    className="w-full flex items-center justify-between px-3 py-2 text-sm font-medium text-gray-700 rounded-lg hover:bg-gray-100"
                  >
                    <span className="flex items-center gap-3">
                      {item.icon}
                      {item.label}
                    </span>
                    <ChevronDown
                      className={cn(
                        "w-4 h-4 transition-transform",
                        expandedItems.includes(item.label) && "rotate-180"
                      )}
                    />
                  </button>
                  {expandedItems.includes(item.label) && (
                    <ul className="ml-8 mt-1 space-y-1">
                      {item.children.map((child) => (
                        <li key={child.label}>
                          <Link
                            href={child.href!}
                            className={cn(
                              "block px-3 py-2 text-sm rounded-lg",
                              isActive(child.href)
                                ? "bg-blue-50 text-blue-600 font-medium"
                                : "text-gray-600 hover:bg-gray-100"
                            )}
                          >
                            {child.label}
                          </Link>
                        </li>
                      ))}
                    </ul>
                  )}
                </>
              ) : (
                <Link
                  href={item.href!}
                  className={cn(
                    "flex items-center gap-3 px-3 py-2 text-sm font-medium rounded-lg",
                    isActive(item.href)
                      ? "bg-blue-50 text-blue-600"
                      : "text-gray-700 hover:bg-gray-100"
                  )}
                >
                  {item.icon}
                  {item.label}
                </Link>
              )}
            </li>
          ))}
        </ul>
      </nav>
    </aside>
  );
}
```

### 5.3 Header 组件（使用 HeroUI Button）

**文件**：`frontend/components/layout/header.tsx`

```typescript
"use client";

import { Search, User, LogOut } from "lucide-react";
import { Button } from "@heroui/react";

export default function Header() {
  const handleLogout = () => {
    localStorage.removeItem("token");
    window.location.href = "/";
  };

  return (
    <header className="h-16 bg-white border-b border-gray-200 flex items-center justify-between px-6">
      <div className="flex items-center gap-4 flex-1 max-w-xl">
        <div className="relative flex-1">
          <Search className="absolute left-3 top-1/2 -translate-y-1/2 w-4 h-4 text-gray-400" />
          <input
            type="text"
            placeholder="搜索..."
            className="w-full pl-10 pr-4 py-2 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500"
          />
        </div>
      </div>

      <div className="flex items-center gap-4">
        <Button
          variant="light"
          startContent={<User className="w-4 h-4" />}
          className="text-gray-700"
        >
          用户中心
        </Button>
        <Button
          variant="light"
          startContent={<LogOut className="w-4 h-4" />}
          onPress={handleLogout}
          className="text-gray-700"
        >
          退出登录
        </Button>
      </div>
    </header>
  );
}
```

### 5.4 PageContainer 组件

**文件**：`frontend/components/layout/page-container.tsx`

```typescript
import { ReactNode } from "react";

interface PageContainerProps {
  children: ReactNode;
  title?: string;
}

export default function PageContainer({ children, title }: PageContainerProps) {
  return (
    <div className="flex-1 overflow-auto bg-gray-50">
      {title && (
        <div className="bg-white border-b border-gray-200 px-6 py-4">
          <h2 className="text-2xl font-bold text-gray-900">{title}</h2>
        </div>
      )}
      <div className="p-6">{children}</div>
    </div>
  );
}
```

### ✅ 验证步骤

```bash
npm run type-check
# 应无类型错误
```

---

## 阶段 6：路由架构（Next.js 14 最佳实践）

### 6.1 创建路由组

```bash
cd frontend/app

# 创建路由组（括号不影响 URL）
mkdir -p "(auth)" "(main)"
```

### 6.2 根布局（保持最小化）

**文件**：`frontend/app/layout.tsx`

```typescript
import type { Metadata } from "next";
import "./globals.css";

export const metadata: Metadata = {
  title: "智创电商营销系统",
  description: "基于 AIGC 的新电商数字化营销技术研究与创新应用",
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="zh-CN">
      <body>{children}</body>
    </html>
  );
}
```

### 6.3 登录页路由（无侧边栏）

**文件**：`frontend/app/(auth)/page.tsx`

```typescript
import LoginPage from "@/components/ui/login-page";
import CanvasBackground from "@/components/ui/canvas";

export default function Home() {
  return (
    <main className="relative h-screen w-full overflow-hidden">
      <CanvasBackground />
      <div className="absolute inset-0 flex items-center justify-center">
        <LoginPage />
      </div>
    </main>
  );
}
```

### 6.4 业务页面布局（带侧边栏）

**文件**：`frontend/app/(main)/layout.tsx`

```typescript
import Sidebar from "@/components/layout/sidebar";
import Header from "@/components/layout/header";

export default function MainLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <div className="flex h-screen">
      <Sidebar />
      <div className="flex-1 flex flex-col">
        <Header />
        {children}
      </div>
    </div>
  );
}
```

### ✅ 验证步骤

```bash
npm run dev

# 访问 http://localhost:3000
# 应看到登录页（无侧边栏）

# 登录后访问 http://localhost:3000/home
# 应看到侧边栏和头部
```

---

## 阶段 7：示范页面（HeroUI v3 + Next.js 14）

### 7.1 创建首页目录

```bash
mkdir -p frontend/app/"(main)"/home
```

### 7.2 首页实现（使用复合组件）

**文件**：`frontend/app/(main)/home/page.tsx`

```typescript
import { Card } from "@heroui/react";
import { CheckCircle } from "lucide-react";
import PageContainer from "@/components/layout/page-container";

export default function HomePage() {
  const features = [
    "知识库+大语言模型",
    "文生图片技术",
    "文生视频技术",
    "数字人直播带货",
    "小红书、微博等平台营销",
  ];

  return (
    <PageContainer>
      <div className="max-w-7xl mx-auto space-y-8">
        {/* 顶部横幅 */}
        <div className="bg-gradient-to-r from-blue-600 to-purple-600 rounded-2xl p-12 text-white">
          <h1 className="text-4xl font-bold mb-4">智创电商营销系统</h1>
          <p className="text-xl opacity-90">
            基于 AIGC 的新电商数字化营销技术研究与创新应用
          </p>
        </div>

        {/* 卡片网格 - HeroUI v3 复合组件模式 */}
        <div className="grid grid-cols-1 md:grid-cols-2 gap-6">
          <Card>
            <Card.Header>
              <h3 className="text-xl font-semibold">项目简介</h3>
            </Card.Header>
            <Card.Body>
              <p className="text-gray-600 leading-relaxed">
                随着生成式人工智能（AIGC）技术的快速发展，电商产业迎来了一次潜在的变革和更新。
                本项目利用 AI 技术加速电商行业的自动化，
                并实现产品文案、宣传图、视频内容的智能化生成，同时借助数字人进行直播带货，从而推动电商产业的转型升级。
              </p>
            </Card.Body>
          </Card>

          <Card>
            <Card.Header>
              <h3 className="text-xl font-semibold">核心功能</h3>
            </Card.Header>
            <Card.Body>
              <ul className="space-y-3">
                {features.map((feature) => (
                  <li key={feature} className="flex items-center gap-2">
                    <CheckCircle className="w-5 h-5 text-green-500 flex-shrink-0" />
                    <span className="text-gray-700">{feature}</span>
                  </li>
                ))}
              </ul>
            </Card.Body>
          </Card>
        </div>
      </div>
    </PageContainer>
  );
}
```

### ✅ 验证步骤

```bash
npm run dev
# 访问 http://localhost:3000/home
# 应看到完整的首页布局
```

---

## 阶段 8：商品管理页面（完整示例）

### 8.1 创建目录

```bash
mkdir -p frontend/app/"(main)"/products
```

### 8.2 商品列表页（遵循 Vercel 最佳实践）

**文件**：`frontend/app/(main)/products/page.tsx`

```typescript
"use client";

import { Button, Card, Spinner } from "@heroui/react";
import { Plus } from "lucide-react";
import Link from "next/link";
import PageContainer from "@/components/layout/page-container";
import { useProducts } from "@/lib/hooks/use-products";
import { formatCurrency, formatDate } from "@/lib/utils/format";

/**
 * 商品列表页
 * 
 * 最佳实践：
 * - 使用 SWR 自动缓存和去重（client-swr-dedup）
 * - 使用 Spinner 组件显示加载状态
 * - 使用 HeroUI Card 复合组件
 */
export default function ProductsPage() {
  const { products, isLoading } = useProducts();

  if (isLoading) {
    return (
      <PageContainer title="商品基础信息库">
        <div className="flex justify-center items-center h-64">
          <Spinner size="lg" />
        </div>
      </PageContainer>
    );
  }

  return (
    <PageContainer title="商品基础信息库">
      <div className="space-y-4">
        <div className="flex justify-end">
          <Button
            as={Link}
            href="/products/new"
            color="primary"
            startContent={<Plus className="w-4 h-4" />}
          >
            新增商品
          </Button>
        </div>

        <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4">
          {products.map((product) => (
            <Card key={product.id} isPressable as={Link} href={`/products/${product.id}`}>
              <Card.Body>
                {product.image_url && (
                  <img
                    src={product.image_url}
                    alt={product.name}
                    className="w-full h-48 object-cover rounded-lg mb-4"
                  />
                )}
                <h3 className="text-lg font-semibold mb-2">{product.name}</h3>
                <p className="text-sm text-gray-500 mb-2">{product.category}</p>
                <p className="text-xl font-bold text-blue-600">
                  {formatCurrency(product.price)}
                </p>
                <p className="text-xs text-gray-400 mt-2">
                  {formatDate(product.created_at)}
                </p>
              </Card.Body>
            </Card>
          ))}
        </div>
      </div>
    </PageContainer>
  );
}
```

### 8.3 Loading 状态

**文件**：`frontend/app/(main)/products/loading.tsx`

```typescript
import { Spinner } from "@heroui/react";
import PageContainer from "@/components/layout/page-container";

export default function Loading() {
  return (
    <PageContainer title="商品基础信息库">
      <div className="flex justify-center items-center h-64">
        <Spinner size="lg" />
      </div>
    </PageContainer>
  );
}
```

### 8.4 Error 边界

**文件**：`frontend/app/(main)/products/error.tsx`

```typescript
"use client";

import { Button } from "@heroui/react";
import PageContainer from "@/components/layout/page-container";

export default function Error({
  error,
  reset,
}: {
  error: Error;
  reset: () => void;
}) {
  return (
    <PageContainer title="商品基础信息库">
      <div className="flex flex-col items-center justify-center h-64 space-y-4">
        <p className="text-red-600">加载失败: {error.message}</p>
        <Button color="primary" onPress={reset}>
          重试
        </Button>
      </div>
    </PageContainer>
  );
}
```

### ✅ 验证步骤

```bash
npm run dev
# 访问 http://localhost:3000/products
# 应看到商品列表或加载状态
```

---

## 📚 关键最佳实践代码块

### 1. 并行数据获取（Vercel 最佳实践）

```typescript
// ❌ 错误：串行获取（瀑布请求）
export default async function Page() {
  const user = await fetchUser();
  const posts = await fetchPosts(); // 等待 user 完成
  return <div>...</div>;
}

// ✅ 正确：并行获取
export default async function Page() {
  const userPromise = fetchUser();
  const postsPromise = fetchPosts();
  
  const [user, posts] = await Promise.all([userPromise, postsPromise]);
  return <div>...</div>;
}
```

### 2. 动态导入重型组件

```typescript
// ❌ 错误：直接导入 ECharts
import ReactECharts from "echarts-for-react";

// ✅ 正确：动态导入
import dynamic from "next/dynamic";

const ReactECharts = dynamic(() => import("echarts-for-react"), {
  ssr: false,
  loading: () => <Spinner />,
});
```

### 3. HeroUI v3 复合组件模式

```typescript
// ❌ 错误：v2 扁平模式
<Card title="标题" description="描述">内容</Card>

// ✅ 正确：v3 复合组件
<Card>
  <Card.Header>
    <Card.Title>标题</Card.Title>
    <Card.Description>描述</Card.Description>
  </Card.Header>
  <Card.Body>内容</Card.Body>
</Card>
```

### 4. 使用 onPress 而非 onClick

```typescript
// ❌ 错误：使用 onClick
<Button onClick={() => console.log("clicked")}>按钮</Button>

// ✅ 正确：使用 onPress（更好的无障碍支持）
<Button onPress={() => console.log("clicked")}>按钮</Button>
```

### 5. Server Components 优先

```typescript
// ✅ 正确：Server Component 获取数据
export default async function Page() {
  const data = await fetch("https://api.example.com/data");
  return <ClientComponent data={data} />;
}

// ❌ 错误：Client Component 获取数据（增加客户端 JS）
"use client";
export default function Page() {
  const [data, setData] = useState(null);
  useEffect(() => {
    fetch("https://api.example.com/data").then(setData);
  }, []);
  return <div>{data}</div>;
}
```

---

## 阶段 9：页面迁移清单

### 9.1 迁移优先级表

| 优先级 | 旧页面 | 新路由 | 目录 | 核心组件 | API 依赖 |
|--------|--------|--------|------|----------|----------|
| 1 | product_marketing.html | /products/marketing | app/(main)/products/marketing | ProductCard, ImageGrid | /api/get_all_marketing_products |
| 2 | dashboard.html | /dashboard | app/(main)/dashboard | StatCard, Charts | /api/dashboard/data |
| 3 | test1.html | /ai-tools/copywriting | app/(main)/ai-tools/copywriting | CopyGenerator, StreamOutput | /api/generate_xiaohongshu_stream_post |
| 4 | test2.html | /ai-tools/image | app/(main)/ai-tools/image | ImageGenerator, TaskStatus | /api/generate_img2img |
| 5 | test3.html | /ai-tools/video | app/(main)/ai-tools/video | VideoGenerator, TaskStatus | /api/generate_video |
| 6 | calendar.html | /marketing/calendar | app/(main)/marketing/calendar | CalendarView, EventForm | 待定 |
| 7 | note.html | /marketing/notes | app/(main)/marketing/notes | NoteEditor, NoteList | 待定 |
| 8 | setting.html | /live | app/(main)/live | LiveControl | /api/run_exe |
| 9 | dashboard_internal.html | /dashboard/internal | app/(main)/dashboard/internal | 同 dashboard | /api/dashboard/data |
| 10 | sign_up.html | /dashboard/sign-up | app/(main)/dashboard/sign-up | StatTable | 待定 |

### 9.2 标准迁移步骤模板

每个页面按以下步骤迁移：

```bash
# 1. 创建目录
mkdir -p frontend/app/"(main)"/[路径]

# 2. 创建 page.tsx
# 3. 创建 loading.tsx
# 4. 创建 error.tsx（如需要）
# 5. 创建业务组件（如需要）
# 6. 创建 API 函数（如需要）
# 7. 验证
npm run dev
```

**page.tsx 模板**：

```typescript
"use client";

import { Spinner } from "@heroui/react";
import PageContainer from "@/components/layout/page-container";
import { use[Domain]Hook } from "@/lib/hooks/use-[domain]";

export default function [Page]Page() {
  const { data, isLoading } = use[Domain]Hook();

  if (isLoading) {
    return (
      <PageContainer title="页面标题">
        <div className="flex justify-center items-center h-64">
          <Spinner size="lg" />
        </div>
      </PageContainer>
    );
  }

  return (
    <PageContainer title="页面标题">
      {/* 页面内容 */}
    </PageContainer>
  );
}
```

---

## 阶段 10：性能优化检查清单

### 10.1 关键性能指标

| 指标 | 目标 | 检查方法 |
|------|------|----------|
| First Load JS | < 200KB | `npm run build` 查看输出 |
| LCP | < 2.5s | Chrome DevTools Lighthouse |
| FID | < 100ms | Chrome DevTools Lighthouse |
| CLS | < 0.1 | Chrome DevTools Lighthouse |

### 10.2 优化检查清单

- [ ] **消除瀑布请求**：使用 `Promise.all()` 并行获取数据
- [ ] **动态导入重型组件**：ECharts、Markdown 编辑器等
- [ ] **使用 SWR 缓存**：避免重复请求
- [ ] **图片优化**：使用 `next/image` 组件
- [ ] **代码分割**：按路由自动分割
- [ ] **使用 Server Components**：减少客户端 JS
- [ ] **避免不必要的 "use client"**：仅在需要交互时使用

### 10.3 性能优化示例

```typescript
// ✅ 正确：并行获取 + 动态导入
import dynamic from "next/dynamic";

const Chart = dynamic(() => import("@/components/charts/revenue-chart"), {
  ssr: false,
  loading: () => <Spinner />,
});

export default async function DashboardPage() {
  // 并行获取数据
  const [revenue, orders] = await Promise.all([
    fetchRevenue(),
    fetchOrders(),
  ]);

  return (
    <div>
      <Chart data={revenue} />
      <OrderList data={orders} />
    </div>
  );
}
```

---

## 阶段 11：验证与部署

### 11.1 本地验证清单

```bash
# 1. 类型检查
npm run type-check

# 2. 构建检查
npm run build

# 3. 启动生产模式
npm run start

# 4. Lighthouse 检查
# 在 Chrome DevTools 中运行 Lighthouse
```

### 11.2 功能验证清单

- [ ] 登录页正常显示（无侧边栏）
- [ ] 登录成功后跳转到 /home
- [ ] 侧边栏导航正常工作
- [ ] 所有页面路由正常
- [ ] API 请求正确代理到后端
- [ ] HeroUI 组件样式正确
- [ ] 加载状态正常显示
- [ ] 错误边界正常工作
- [ ] 无 TypeScript 错误
- [ ] 无浏览器 console 错误

### 11.3 常见问题排查

**问题 1：HeroUI 样式不生效**

```bash
# 检查 globals.css 导入顺序
cat app/globals.css
# 必须先 @import "tailwindcss"，再 @import "@heroui/styles"
```

**问题 2：Tailwind v4 编译错误**

```bash
# 确认已删除 tailwind.config.js
ls tailwind.config.js
# 应显示：No such file or directory

# 确认 postcss.config.mjs 正确
cat postcss.config.mjs
```

**问题 3：API 请求 404**

```bash
# 检查 next.config.js rewrites
cat next.config.js

# 确认后端服务已启动
curl http://localhost:5000/api/get_products
```

**问题 4：组件导入错误**

```typescript
// ❌ 错误：从 @heroui/system 导入（v2）
import { Card } from "@heroui/system";

// ✅ 正确：从 @heroui/react 导入（v3）
import { Card } from "@heroui/react";
```

### 11.4 部署前检查

```bash
# 1. 环境变量配置
cat .env.local
# 确认包含：
# NEXT_PUBLIC_API_URL=http://localhost:5000
# NEXT_PUBLIC_LOGIN_URL=http://localhost:3002

# 2. 构建优化
npm run build
# 检查输出的包大小

# 3. 生产模式测试
npm run start
# 访问 http://localhost:3000 测试所有功能
```

---

## 📖 附录：规范与约束

### A. 命名规范

| 类型 | 规范 | 示例 |
|------|------|------|
| 组件文件 | kebab-case.tsx | `sidebar.tsx`, `page-container.tsx` |
| 组件名称 | PascalCase | `Sidebar`, `PageContainer` |
| Hook 文件 | use-*.ts | `use-auth.ts`, `use-products.ts` |
| API 文件 | kebab-case.ts | `auth.ts`, `products.ts` |
| 类型文件 | kebab-case.ts | `api.ts`, `product.ts` |
| 常量 | UPPER_SNAKE_CASE | `API_BASE_URL`, `ROUTES` |

### B. 目录结构规范

```
frontend/
├── app/                    # Next.js App Router
│   ├── (auth)/            # 登录路由组
│   ├── (main)/            # 业务路由组
│   ├── layout.tsx         # 根布局
│   └── globals.css        # 全局样式
├── components/            # 组件
│   ├── layout/           # 布局组件
│   ├── charts/           # 图表组件
│   └── [domain]/         # 领域组件
├── lib/                   # 库
│   ├── api/              # API 层
│   ├── hooks/            # Hooks 层
│   ├── stores/           # 状态管理
│   ├── types/            # 类型定义
│   └── utils/            # 工具函数
└── public/               # 静态资源
```

### C. 代码规范

1. **使用 TypeScript 严格模式**
2. **所有组件必须有类型定义**
3. **使用 ESLint 和 Prettier**
4. **遵循 React Hooks 规则**
5. **使用 HeroUI v3 复合组件模式**
6. **使用 onPress 而非 onClick**
7. **优先使用 Server Components**

---

## 🎉 总结

本指南提供了完整的前端重构路径：

1. ✅ **阶段 0-1**：诊断、清理、安装依赖
2. ✅ **阶段 2-4**：基础设施、API 层、Hooks 层
3. ✅ **阶段 5-6**：布局组件、路由架构
4. ✅ **阶段 7-8**：示范页面、商品管理
5. ✅ **阶段 9**：页面迁移清单
6. ✅ **阶段 10-11**：性能优化、验证部署

**关键成果**：
- 统一技术栈：HeroUI v3 + Tailwind CSS v4 + Next.js 14
- 遵循最佳实践：Vercel React + Next.js 官方指南
- 完整的代码示例和验证步骤
- 清晰的规范和约束

按顺序执行即可完成前端重构。

